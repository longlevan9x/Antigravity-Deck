# CHANGELOG

## [2024-03-24] - Telegram Logging & Unified Event Hook

### Modified Files:

#### 📂 `src/agent-bridge.js`
- **Updated `eventHook` function:**
  - Added support for the `'log'` event: now correctly forwards logs from transport modules (Telegram/Discord) to the central `addLog` system.
  - Added support for the `'update'` event: automatically formats and logs incoming messages from transport relays as system logs.

#### 📂 `src/telegram-relay.js`
- **Updated `bot.on('text')` handler:**
  - Replaced all invalid `addLog` calls with the correct `eventHook('log', ...)` method.
  - Added a diagnostic log at the very start of the message reception flow (`[Telegram] Start Received message`).
  - Added diagnostic logs before and after the interaction with the AI engine (`Before handlePiReply` and `After handlePiReply`).
- **Updated `handleCommandWrap` function:**
  - Added a log to track when commands (like `/help`, `/status`) are initiated.

---
*Generated by Antigravity*
