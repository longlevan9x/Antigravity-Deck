# CHANGELOG

## [2024-03-24] - Telegram Logging & Unified Event Hook

### Modified Files:

#### 📂 `src/agent-bridge.js`
- **Enhanced `eventHook` system:**
  - Restored and standardized logging for multiple event types across all transports: `error`, `ready`, `listening`, `log`, `update`, `reply`, `command`, and `ignored`.
  - Implemented detailed message formatting (e.g., `[Transport] Msg from @User: "Text"`) for better traceability in `bridge.log`.
  - Ensured the system remains transport-agnostic by using the `transportName` parameter for all logs.

#### 📂 `src/telegram-relay.js`
- **Interactive UI Elements:**
  - Added `getMainMenu()` helper to generate an inline keyboard with common actions (Status, Logs, List Workspaces, Accept, Reject, Abort, Help).
  - Updated `bot.start` to display the inline menu upon activation.
  - Implemented `bot.action` handlers to process button clicks from the inline keyboard.
- **Command Discovery:**
  - Integrated `bot.telegram.setMyCommands` to register core commands with the Telegram UI (appears as a "Menu" button next to the text box).
- **Diagnostics:**
  - Added a log to `handleCommandWrap` to track when commands (like `/help`, `/status`) are initiated.
- **Unified Event Hook:**
  - Replaced all invalid `addLog` calls with the correct `eventHook('log', ...)` method.
  - Added diagnostic logs (`[Telegram] Start Received message`, `Before handlePiReply`, `After handlePiReply`).

---
*Generated by Antigravity*
